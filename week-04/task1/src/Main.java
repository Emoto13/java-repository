import java.util.List;

import bg.sofia.uni.fmi.mjt.flightscanner.FlightScanner;
import bg.sofia.uni.fmi.mjt.flightscanner.flight.*;
import bg.sofia.uni.fmi.mjt.flightscanner.airport.*;

public class Main {
    public static void main(String[] args) {
        /*FlightScanner flightScanner = new FlightScanner();
        flightScanner.add(RegularFlight.of("a", new Airport("A"), new Airport("B"), 10));
        flightScanner.add(RegularFlight.of("a", new Airport("A"), new Airport("Ada"), 10));
        //List<Flight> res = flightScanner.searchFlights(new Airport("A"), new Airport("B"));
        //for (Flight flight : res) {
        //    System.out.println(flight);
        //}
        
        System.out.println(flightScanner.getFlightsSortedByDestination(new Airport("A")));*/
    }
}